This is reward service project

To start the project you need to follow these steps:
- composer install
- php artisan key:generate
- php artisan migrate

To see the API documentation you need to open /docs/api
